# openvela GD32H7 工程 · 云服务器部署与编译完整操作说明

> 适用范围：在 **seetacloud（AutoDL）云服务器**上，从零部署 openvela SDK + 斯巴克氖龍实验室增量代码，完整编译出可烧录固件。
> 本说明按 **2026-09-24 实测通过** 的完整流程编写（128 核服务器，`make -j128` 43 秒出固件，rc=0）。

---

## 0. 部署结果总览（本次实测）

| 项目 | 结果 |
|---|---|
| 操作系统 | Ubuntu 22.04.3 LTS（128 vCPU） |
| SDK 工作区 | `/root/autodl-tmp/vela`（5.7G） |
| 编译命令 | `configure.sh` + `make olddefconfig` + `make -j128` |
| 编译耗时 | 约 43 秒（20:55:12 → 20:55:40） |
| 固件 | `nuttx.bin` 591,824 B（flash 28.22%）+ `nuttx.hex` 1,664,722 B |
| 内存占用 | flash 591,824 B / 2 MB = 28.22%；sram 23,796 B / 512 KB = 4.54% |
| 关键配置 | `CONFIG_ARCH_ARMV7M=y`、`CONFIG_ARCH_CORTEXM7=y`、`CONFIG_GD32H759IMT6_GT911=y`、`CONFIG_EXAMPLES_GUI/GT911/LCD/LEDS=y` |
| 服务器脚本/日志 | `/root/autodl-tmp/vela/build_server_fix.sh`、`/root/autodl-tmp/vela/build.log` |

---

## 1. 服务器信息与连接

**实例**：seetacloud（AutoDL）AI 云，Ubuntu 22.04，128 核。

```bash
ssh -p 52170 root@connect.westb.seetacloud.com
# 密码：由用户提供（文档中不落明文，防止仓库泄露；输入时以终端提示为准）
```

**磁盘布局（本次实测）**：

| 挂载点 | 容量 | 用途 |
|---|---|---|
| `/`（overlay，系统盘） | 30G（初始 89% 满，部署后 53%） | 系统 |
| `/root/autodl-tmp`（`/dev/md0`，数据盘） | 50G（部署后 27G 用 / 24G 可用） | **SDK 与工程全部放这里**（系统盘空间紧张） |

**服务器上保留的工程**：

```
/root/autodl-tmp/
├── vela/           # openvela SDK + 增量 + 编译脚本（本说明主体）
└── waft_stereo/    # waft 工程（21G，按要求保留；cdanet/mafnet 等已删除）
```

---

## 2. 环境准备（依赖 + repo 工具）

```bash
apt update
apt install -y git python3 python3-pip build-essential libncurses5-dev \
    libssl-dev unzip wget curl file

# repo 工具（清华 git-repo 源，避免 GitHub 超时）
mkdir -p /usr/local/bin
curl -s https://mirrors.tuna.tsinghua.edu.cn/git/git-repo/repo > /usr/local/bin/repo
chmod a+x /usr/local/bin/repo
repo --version   # 期望：repo launcher version 2.65（本次实测）
```

---

## 3. 拉取 openvela SDK（gitee 官方镜像，赛题分支）

> **关键决策**：服务器直连 GitHub 主站实测 HTTP 000 超时（raw 约 17KB/s），**必须用 gitee 官方组织镜像**。
> 已确认 `gitee.com/open-vela` 镜像包含 `dev-ai-contest-2026` 分支的 manifests / nuttx / vendor_gigadevice / prebuilts 全部仓库。

```bash
mkdir -p /root/autodl-tmp/vela && cd /root/autodl-tmp/vela

# 初始化（gitee 镜像 + 清华 repo 源）
repo init -u https://gitee.com/open-vela/manifests.git \
    -b dev-ai-contest-2026 -m openvela.xml \
    --repo-url=https://mirrors.tuna.tsinghua.edu.cn/git/git-repo/ --git-lfs

# 同步（gitee 实测约 1GB/分钟，-j16）
repo sync -c -j16
```

**同步后的两个已知问题（均不影响编译）**：

| 现象 | 处理 |
|---|---|
| `Cannot checkout prebuilts_gcc_linux_tricore` | **可忽略**——TC（Tricore）单片机工具链，与 GD32H7 无关 |
| `nuttx/` 工作树不完整（如 `arch/arm/src/` 顶层文件缺失，make 报 `No rule to make target 'arm_cache.c'`） | 在 `/root/autodl-tmp/vela` 下执行 `repo sync -c -j16 nuttx` 重拉 nuttx；仍不完整则用第 6 步的完整内核包覆盖（本次实测采用） |

**检查工具链**：

```bash
ls /root/autodl-tmp/vela/prebuilts/gcc/linux-x86_64/arm-none-eabi/bin/arm-none-eabi-gcc
# 期望：存在（arm-none-eabi-gcc 13.4.0）
# 若缺：repo sync -c -j8 -g notdefault,platform-linux
```

---

## 4. kconfig-frontends 工具就位（关键步骤）

openvela 的 kconfig 工具 `kconfig-conf` 是**动态链接**的，需要：

1. **二进制在**：`prebuilts/kconfig-frontends/bin/kconfig-conf`
2. **动态库路径**：`prebuilts/kconfig-frontends/lib/libkconfig-parser-4.11.0.so`

```bash
ls /root/autodl-tmp/vela/prebuilts/kconfig-frontends/bin/kconfig-conf
# 若不存在（某些 manifest 版本不随默认组拉取）：
cd /root/autodl-tmp/vela && repo sync -c -j8 -g notdefault,platform-linux
# 仍没有：在 openvela 根目录先跑一次 ./build.sh <defconfig> -j4（会自动编译 kconfig-frontends）
# 兜底（本次实测采用）：上传 kconfig-frontends.tar.gz（2.5MB）到 /root/autodl-tmp/vela 解压覆盖到
# prebuilts/kconfig-frontends/，核对 bin/kconfig-conf 与 lib/libkconfig-parser-4.11.0.so 两个文件在位
```

> ⚠️ **每次编译前必须设置**（否则 configure 报 `libkconfig-parser-4.11.0.so: cannot open shared object file`）：
> ```bash
> export LD_LIBRARY_PATH=/root/autodl-tmp/vela/prebuilts/kconfig-frontends/lib:$LD_LIBRARY_PATH
> ```

---

## 5. 拉取自建交付仓库（增量补丁 + 完整芯片层/板卡层）

> ⚠️ **本次实测：服务器直连 GitHub 超时（HTTP 000）**，因此增量以 **tar 包上传（方式 B）** 为主路径；
> 若你的服务器访问 GitHub 正常，可直接用 git 方式（如下）。

**方式 git（GitHub 可达时）：**

```bash
cd /root/autodl-tmp/vela
git clone https://github.com/QQQuer/GD32H7-Openvela.git
cd GD32H7-Openvela
git checkout dev-ai-contest-final    # 交付分支
```

**方式 tar（本次实测路径，服务器 GitHub 受限时）：**

在**本地/VM** 打包以下 4 个文件，用 SFTP/scp 上传到 `/root/autodl-tmp/vela/`：

| 包 | 大小 | 内容 |
|---|---|---|
| `gd32h7_incremental.tar.gz` | 4.2MB | board/contest_board（芯片层+板卡层）+ app（4 个应用）+ firmware + README |
| `gd32h7_include.tar.gz` | 6.5KB | `gd32h7xx/` 3 个文件（chip.h / gd32h7xx_irq.h / irq.h） |
| `kconfig-frontends.tar.gz` | 2.5MB | kconfig 工具（第 4 步兜底用） |
| `nuttx_vm.tar.gz` | 297.7MB | VM 全量 nuttx 内核（gitee nuttx 不完整时覆盖用） |

> 上传命令示例（Windows PowerShell）：`scp -P 52170 gd32h7_incremental.tar.gz root@connect.westb.seetacloud.com:/root/autodl-tmp/vela/`
> 解压与复制见第 6 步方式 B。

---

## 6. 应用增量到 SDK（芯片层 + 板卡层 + 应用）

**方式 A：从 git 仓库整体复制**

```bash
cd /root/autodl-tmp/vela
SRC=GD32H7-Openvela

# ① 完整 GD32H7 芯片层（45 个文件：start/irq/serial/rcu/lowputc/timerisr/hardware 等）
cp -r $SRC/board/contest_board/nuttx/arch/arm/src/gd32h7xx nuttx/arch/arm/src/

# ② include 目录（3 个文件：chip.h / gd32h7xx_irq.h / irq.h，仓库已含）——缺它 configure 会失败
mkdir -p nuttx/arch/arm/include/gd32h7xx
cp -r $SRC/board/contest_board/nuttx/arch/arm/include/gd32h7xx nuttx/arch/arm/include/

# ③ 完整板卡层（30 个文件：src/configs/scripts/include）
mkdir -p vendor/gigadevice/boards/gd32h7
cp -r $SRC/board/contest_board/vendor/gigadevice/boards/gd32h7/gd32h759imt6 vendor/gigadevice/boards/gd32h7/

# ④ GUI 应用
cp -r $SRC/app/gui apps/examples/gui

# ⑤ GT911 / LCD / LED 示例
cp -r $SRC/app/gt911_demo apps/examples/gt911
cp -r $SRC/app/lcd_demo  apps/examples/lcd
mkdir -p apps/examples/leds
cp $SRC/app/leds_demo/leds_main.c apps/examples/leds/
```

**方式 B：从 tar 包解压（服务器网络受限时，本次实测采用）**

```bash
cd /root/autodl-tmp/vela
tar xzf gd32h7_incremental.tar.gz
tar xzf gd32h7_include.tar.gz     # 解压出顶层 gd32h7xx/ 目录（chip.h / gd32h7xx_irq.h / irq.h）

# 手动复制到目标位置（与方式 A 的 ①~⑤ 相同动作）：
cp -r board/contest_board/nuttx/arch/arm/src/gd32h7xx nuttx/arch/arm/src/
mkdir -p nuttx/arch/arm/include
cp -r gd32h7xx nuttx/arch/arm/include/
mkdir -p vendor/gigadevice/boards/gd32h7
cp -r board/contest_board/vendor/gigadevice/boards/gd32h7/gd32h759imt6 vendor/gigadevice/boards/gd32h7/
cp -r app/gui apps/examples/gui
cp -r app/gt911_demo apps/examples/gt911
cp -r app/lcd_demo apps/examples/lcd
mkdir -p apps/examples/leds
cp app/leds_demo/leds_main.c apps/examples/leds/
```

> 若 gitee 的 nuttx 工作树不完整（见第 3 步），先把 `nuttx_vm.tar.gz` 解压覆盖：
> ```bash
> cd /root/autodl-tmp/vela
> rm -rf nuttx && mkdir nuttx && tar xzf nuttx_vm.tar.gz -C nuttx --strip-components=1
> ```

---

## 7. Kconfig 兼容修复（老 kconfig 工具 + 新 SDK 的兼容层）

openvela 分支的 Kconfig 与服务器上 kconfig-frontends 4.11 不完全兼容，需执行一次修复。修复脚本 **已随仓库提供**（`deploy/fix_kconfig_server.sh`，克隆到 `GD32H7-Openvela/deploy/` 或从增量包同目录取），先放到 vela 根再执行：

```bash
cp /root/autodl-tmp/vela/GD32H7-Openvela/deploy/fix_kconfig_server.sh /root/autodl-tmp/vela/   # git 方式
# 或：cp $V/GD32H7-Openvela/deploy/fix_kconfig_server.sh $V/  （增量包解压后同结构）
bash /root/autodl-tmp/vela/fix_kconfig_server.sh
```

修复内容（四步）：

| # | 修复 | 说明 |
|---|---|---|
| 1 | **批量规范化 help 块**（约 431 个 Kconfig 文件） | 老 kconf 对非 `---help---` 起始的 help 文本要求制表符缩进/合并，遍历全部 Kconfig 自动修复 |
| 2 | `arch/tricore/Kconfig` 的 `--help--` → `---help---` | 官方分支 typo，与 GD32H7 无关但会阻断 kconf 解析 |
| 3 | 清空 `drivers/hwtracing/tricoreht/Kconfig` 与 `coresight/Kconfig` | 内容引用老 kconf 不支持的语法，置空规避 |
| 4 | 注释 `apps/graphics/lvgl/Kconfig` 的 `osource` 行 | 该行引用的子模块在 SDK 中不存在，禁用 |

> 脚本幂等，重复执行无害。执行后输出 `=== FIX DONE ===`。

---

## 8. 编译（完整干净流程）

成功编译脚本 `build_server_fix.sh` 同样随仓库提供（`deploy/build_server_fix.sh`），先复制到 vela 根再一键执行：

```bash
cp /root/autodl-tmp/vela/GD32H7-Openvela/deploy/build_server_fix.sh /root/autodl-tmp/vela/
bash /root/autodl-tmp/vela/build_server_fix.sh
```

**脚本等价的手工命令**（务必按此顺序）：

```bash
V=/root/autodl-tmp/vela
export PATH="$V/prebuilts/kconfig-frontends/bin:$V/prebuilts/gcc/linux-x86_64/arm-none-eabi/bin:/usr/bin:/bin"
export LD_LIBRARY_PATH="$V/prebuilts/kconfig-frontends/lib:$LD_LIBRARY_PATH"

cd "$V/nuttx"

# ① 彻底清理（必须，避免旧 .o/.depend 残留导致"假编译"）
make distclean
rm -f .config nuttx.bin nuttx.hex

# ② 配置板卡
./tools/configure.sh ../vendor/gigadevice/boards/gd32h7/gd32h759imt6/configs/nsh

# ③ 生成完整配置
make olddefconfig

# ④ 关键配置自检（四项必须齐全）
grep -E '^CONFIG_ARCH_CHIP=|^CONFIG_ARCH_CORTEXM7=|^CONFIG_ARCH_ARMV7M=|^CONFIG_GD32H759IMT6_GT911=' .config

# ⑤ 编译（128 核实测 43 秒）
make -j128
```

**成功标志**：

```
Memory region         Used Size  Region Size  %age Used
           flash:      591824 B         2 MB     28.22%
            sram:       23796 B       512 KB      4.54%
LD: nuttx
CP: nuttx.hex
CP: nuttx.bin
make rc=0
```

产物：
- `/root/autodl-tmp/vela/nuttx/nuttx.bin`（591,824 B）
- `/root/autodl-tmp/vela/nuttx/nuttx.hex`（1,664,722 B）

> 仅有的警告为无害项：`gui_main.c` 数个 unused 变量、`gd32h7xx_gt911.c:346 "/*" within comment`，不影响功能。

---

## 9. 固件下载到本地

从 Windows 用 SFTP（或 scp）拉取：

```bash
# Windows PowerShell 示例
scp -P 52170 root@connect.westb.seetacloud.com:/root/autodl-tmp/vela/nuttx/nuttx.bin C:\Users\QQQ\Desktop\openvela\nuttx_server.bin
scp -P 52170 root@connect.westb.seetacloud.com:/root/autodl-tmp/vela/nuttx/nuttx.hex C:\Users\QQQ\Desktop\openvela\nuttx_server.hex
```

**下载后必验**（向量表 + 完整性）：

```bash
# 服务器端 md5（对照用）
md5sum /root/autodl-tmp/vela/nuttx/nuttx.bin

# 本地校验向量表（SP 应为 0x2400xxxx，Reset 应为 0x0800xxxx）
xxd -l 16 nuttx_server.bin
```

本次实测：
- 服务器 md5：`d94523e526b34f1dbeb97bd1ae6d6487`
- 初始 SP：`0x240060F4`（SRAM ✅）
- Reset 向量：`0x080013CD`（flash ✅）

---

## 10. 烧录

用 GD32 All In One Programmer（Windows GUI）：
1. 板卡 BOOT0=1 进入 ISP 模式，串口连接（57600 8E1）
2. 选 Part = GD32H759IMT6，Start address = `0x08000000`
3. 烧录 `nuttx_server.bin`（勾选 Verify）
4. 烧完拨回 BOOT0=0，上电，串口 115200 进 NSH：`gui` 启动可视化系统

---

## 11. 服务器文件清单（部署后现状）

```
/root/autodl-tmp/vela/
├── nuttx/                        # 内核（本次为 VM 全量覆盖的非 git 目录，891M）
├── apps/  vendor/  prebuilts/    # SDK 组件
├── GD32H7-Openvela/              # 自建交付仓库（git 方式时；含 deploy/ 脚本 + include 目录）
├── build.sh                      # openvela 官方构建脚本
├── build_server_fix.sh           # ✅ 成功编译脚本（一键；仓库 deploy/ 有备份）
├── fix_kconfig_server.sh         # ✅ Kconfig 修复脚本（一键；仓库 deploy/ 有备份）
├── build.log                     # ✅ 本次编译日志
├── gd32h7_incremental.tar.gz     # 增量包（4.2MB）
├── gd32h7_include.tar.gz         # include 目录包（6.5KB）
├── kconfig-frontends.tar.gz      # kconfig 工具包（2.5MB）
└── nuttx_vm.tar.gz               # VM 全量内核包（297.7MB，备用）

/root/autodl-tmp/waft_stereo/     # waft 工程（21G，保留）
```

> **仓库侧对应内容**（`QQQuer/GD32H7-Openvela` 的 `dev-ai-contest-final` 分支）：
> - `board/contest_board/nuttx/arch/arm/src/gd32h7xx/` —— 芯片层 45 文件
> - `board/contest_board/nuttx/arch/arm/include/gd32h7xx/` —— include 3 文件（chip.h/gd32h7xx_irq.h/irq.h）
> - `board/contest_board/vendor/gigadevice/boards/gd32h7/gd32h759imt6/` —— 板卡层 30 文件
> - `app/` —— gui/gt911_demo/lcd_demo/leds_demo（编译所需 4 应用；hello_app 为附加示例，defconfig 未启用可不复制）
> - `deploy/` —— build_server_fix.sh / fix_kconfig_server.sh
> - `firmware/` —— 全部历史固件 + 服务器编译版 nuttx_server.bin/.hex
> - `deliverables/` —— 技术报告/答辩PPT/海报/演示视频/提交zip

---

## 12. 常见问题排查

| 现象 | 根因 | 处理 |
|---|---|---|
| `error while loading shared libraries: libkconfig-parser-4.11.0.so` | 缺 `LD_LIBRARY_PATH` | 编译前 `export LD_LIBRARY_PATH=$V/prebuilts/kconfig-frontends/lib:$LD_LIBRARY_PATH` |
| `No rule to make target 'arm_cache.c'` | kconfig 失败 → `.config` 未正确生成 → `ARCH_SUBDIR` 为空 → VPATH 缺 armv7-m | `make distclean` 后重跑第 8 步①②③④（先确认 LD_LIBRARY_PATH） |
| `configure.sh` 报 `No directory at arch/arm/include/gd32h7xx` | include 目录未复制（增量包漏项） | 执行第 6 步②复制 include 3 文件 |
| Kconfig 语法错（help/osource/tricoreht） | kconfig 工具版本与 SDK 不匹配 | 跑第 7 步修复脚本；或改用 `./build.sh <defconfig> -j4`（自动编译匹配版本） |
| `Cannot checkout prebuilts_gcc_linux_tricore` | gitee 镜像缺 TC 工具链 | **忽略**（与 GD32H7 无关） |
| 编译 15 秒就"成功"但固件大小异常 | 旧 `.o`/`.depend` 残留空跑 | `make distclean` 后真正干净编译 |
| 服务器访问 GitHub 超时 | 网络限制 | 拉 SDK 用 gitee 镜像；拉增量用 tar 包上传 |

---

## 13. 完成标准（对照自检）

- [ ] `repo sync` 完成，工具链 13.4.0 存在
- [ ] kconfig-conf 存在且 `LD_LIBRARY_PATH` 已设置
- [ ] 芯片层 45 文件 + include 3 文件 + 板卡层 30 文件 + 4 个应用复制到位（对照第 6 步①②③④⑤）
- [ ] 修复脚本输出 `=== FIX DONE ===`
- [ ] `make distclean` → `configure.sh` rc=0 → `olddefconfig` rc=0 → 四项关键配置齐全
- [ ] `make -j128` rc=0，flash 28.22%
- [ ] `nuttx.bin` 下载本地，向量表 SP=0x2400xxxx / Reset=0x0800xxxx，md5 与服务器一致
- [ ] 可烧录进板卡并进入 `gui` 主界面
